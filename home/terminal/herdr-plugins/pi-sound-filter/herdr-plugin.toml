id = "zarred.pi-sound-filter"
name = "Pi Sound Filter"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Replay Herdr sounds for normal Pi agents while muting subagents"
platforms = ["linux"]

[[events]]
on = "pane.agent_status_changed"
command = ["bash", "sound-filter.sh"]
